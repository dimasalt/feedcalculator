import * as z from 'zod';

//<!-- data validations -->
const Validation = async (item:any, schema: z.Schema) : Promise<string[]> => {

    try {
        await schema.parse(item);      
    }
    catch(err: any){
        return [err.issues[0].message];
    }

    return [];
    
}

export default Validation;

